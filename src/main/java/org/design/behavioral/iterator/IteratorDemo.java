package org.design.behavioral.iterator;
/*
数据结构
	逻辑结构：树、队列、堆等
	物理结构：数组、链表
二叉树底层既可以用数组实现，也可以用链表实现。

iterator
对于了解内部构造的容器，可以遍历。对于不了解内部构造并且内部构造比较复杂的对象如何遍历？
为什么用内部类？
	防止被外部调用
	这个iterator实现类让外部看到没有意义，跟其他类没有任何关系。
	模仿jdk，jdk也是用内部类实现
泛型版本<T>. Type/Key/Element
 */
public class IteratorDemo {
}
